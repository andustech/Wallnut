<div id="content" class="w-full page-content instructions-page">
    <div class="container">
        <h1 class="md:mb-6 mb-4 md:pb-4 pb-2 border-b text-black h3">{{ page_title }}</h1>
        {% section 'instructions-pdf' %}
        {% section 'instructions-videos' %}
        {% section 'removal-videos' %}
    </div>
</div>